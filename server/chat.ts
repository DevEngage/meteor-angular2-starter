
import {Meteor} from 'meteor/meteor';

// import {Chat} from '../collections/Chat';

// Meteor.publish('chat', function(options: Object, location: string) {
// //   Counts.publish(this, 'numberOfParties',
//     // Parties.find(buildQuery.call(this, null, location)), { noReady: true });
//   return Chat.find(buildQuery.call(this, null, location), options);
// });
